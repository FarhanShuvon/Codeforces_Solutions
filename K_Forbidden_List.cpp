#include <bits/stdc++.h>
using namespace std;
int main()
{
    int x, n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    int a = x + 1, b = x + 1;
    int cnt = 0;
    int i = 0;
    while (1)
    {
        i = 4;
        cnt = 4;
        if (cnt != 0)
            break;
    }
    cout << i << endl;
}