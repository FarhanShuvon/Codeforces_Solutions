#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    sort(arr, arr + n);
    int m;
    cin >> m;
    int ar[m];
    for (int i = 0; i < m; i++)
    {
        cin >> ar[i];
    }
    sort(ar, ar + n);

    
}